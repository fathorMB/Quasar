import { default as React } from 'react';
/**
 * Forcefully locks the scroll on the page and common layout containers.
 * Useful for immersive pages like maps or editors.
 */
export declare const PageScrollLock: React.FC;
